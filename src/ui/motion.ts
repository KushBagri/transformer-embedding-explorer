// Shared motion language so every reveal feels like one considered piece.

/** Expensive-feeling ease (expo-out-ish). Use for entrances/reveals. */
export const EASE_OUT = [0.22, 1, 0.36, 1] as const
